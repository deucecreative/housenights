# Keep secrets out of the repo (certs/keys live alongside wallet config).
# Allow only the placeholder image directory through so deployments can
# render passes without needing branded art pushed separately.
*
!.gitignore
!apple/
!apple/images/
!apple/images/**
